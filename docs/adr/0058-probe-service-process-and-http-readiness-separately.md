# 托盘分别探测服务进程与 HTTP 就绪状态

Windows 托盘先通过服务管理器判断 ScriptBoard 服务进程状态，再请求最终配置 URL 的匿名 `/login` 页面确认 HTTP 已就绪。进程存在但页面尚不可用时显示“启动中”，连续失败超过容忍窗口后显示“异常”并提供日志入口；“打开 ScriptBoard”只在就绪后启用。不新增匿名 `/health` 路由，以保持已确认的匿名入口最小化。
