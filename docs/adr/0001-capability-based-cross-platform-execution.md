# 采用基于宿主能力的跨平台执行模型

ScriptBoard 程序可以面向 Windows 和 Linux 构建，但单个实例只执行宿主机实际可用解释器所覆盖的脚本类型。每种扩展名对应可配置的执行器链，`.sh` 默认优先使用 `bash`；只有候选执行器不可用或进程无法启动时才回退，脚本一旦启动就不因非零退出、超时或取消而自动重跑。我们不通过内置虚拟机或容器保证 `.sh`、`.py`、`.ps1`、`.bat` 和 `.cmd` 在每台主机上全部可运行，从而保持单进程、轻量部署，同时准确暴露平台限制。
