---
status: superseded by ADR-0023
---

# 信任管理员提供的脚本

ScriptBoard 将管理员上传并启动的脚本视为可信代码，不提供抵御恶意脚本的应用级沙箱。脚本继承运行 ScriptBoard 的专属低权限系统账号之权限；这保留了本地脚本的完整能力和轻量单进程部署，但意味着部署者必须用操作系统账号、文件权限和网络策略建立安全边界。若未来需要执行不可信代码，应引入独立隔离执行器，而不能把现有执行路径描述为安全沙箱。
