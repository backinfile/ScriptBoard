# 使用脚本所在目录作为工作目录

普通主机脚本执行时将脚本所在目录设为进程工作目录。这样脚本的相对路径读写符合本地直接运行时的常见预期，同目录的输入与生成文件也可在文件页中直接查看。一次性执行可按 [ADR-0120](./0120-separate-one-time-source-ownership-from-workdir.md) 选择主机工作目录；两类工作目录都必须是服务身份可访问、不受保护且不经过链接或重解析点的真实目录。
