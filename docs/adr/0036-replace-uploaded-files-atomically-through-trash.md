# 同名上传需确认并通过回收站原子替换

上传目标与现有路径同名时默认跳过，并让管理员在提交前选择跳过、覆盖或重命名；同一批上传把该选择应用到全部冲突项，重命名使用界面预告的可用名称。选择覆盖后，系统先在目标目录写入随机临时普通文件并完成大小校验，再将旧文件移入回收站并以原子重命名提交新文件；失败时尽力恢复旧内容。运行中的脚本、目录、受限链接和其他特殊文件不能被上传覆盖，但仍可跳过或改名，从而避免半写文件、静默覆盖和执行路径竞争。
