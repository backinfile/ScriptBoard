# 区分页面重连与服务重启后的进程恢复

浏览器刷新或网络中断后，客户端通过执行 ID 和日志偏移重新建立 SSE，并继续获取磁盘日志与实时状态。ScriptBoard 服务进程重启时，之前处于活动状态的执行改为 `disconnected`：可继续查看已经落盘的日志，但不保证重新接管或停止原进程，也无法可靠取得最终退出码。MVP 不引入持久执行代理，以保持单进程部署；`disconnected` 与脚本自身失败明确区分。
