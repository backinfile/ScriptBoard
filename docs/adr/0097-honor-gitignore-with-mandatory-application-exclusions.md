# 遵循 gitignore 并强制排除应用保留内容

版本保护遵循仓库 `.gitignore`，被忽略的符合大小文件标记为“已忽略，未受保护”，不使用 `git add -f`；已经跟踪的文件后来被忽略仍继续跟踪，符合 Git 常规语义。`.gitignore` 本身可作为普通文本受保护。`.git/`、回收站和上传临时文件由 ScriptBoard 强制排除且用户不能取消，文件详情区分过大、被忽略、特殊类型或功能停用等未保护原因。
