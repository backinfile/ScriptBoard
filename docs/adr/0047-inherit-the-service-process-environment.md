# 脚本继承服务进程环境

所有脚本直接继承 ScriptBoard 服务进程的环境变量，与运行身份继承模型保持一致；应用不自动过滤、回显或写入日志。ScriptBoard 中预存的普通变量只通过参数引用解析，不自动注入环境。进程可额外获得 `SCRIPTBOARD_RUN_ID` 和 `SCRIPTBOARD_SCRIPT_PATH` 等非敏感运行元数据；部署者必须避免把不希望受信脚本读取的秘密放入服务环境。
