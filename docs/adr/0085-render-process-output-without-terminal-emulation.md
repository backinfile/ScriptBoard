# 进程输出按安全日志展示而非终端模拟

ScriptBoard 不分配伪终端、不接受网页 stdin，也不解析 ANSI 颜色、清屏或光标控制序列。页面用固定样式区分 stdout/stderr，保留换行、回车和 Tab，其他控制字符转义。输出优先按 UTF-8 展示；Windows 上的非 UTF-8 输出回退到当前系统 ANSI 代码页，以兼容 Windows PowerShell、批处理和本地程序的默认输出。仍无法转换的字节以替换字符展示并标记编码错误。持久事件始终保留原始输出字节及来源供下载分析。MVP 不引入 xterm.js，交互式脚本不在支持范围内。
