# 默认推荐原生服务部署而非 Docker

ScriptBoard 默认以 Windows 服务或 Linux 系统服务在宿主机原生运行，并提供开机自启集成，不建议把 Docker 作为首选部署方式。文件管理、进程树控制、宿主解释器发现和 Windows 托盘都依赖明确的宿主能力与权限；容器会增加路径映射、用户映射和宿主控制代理等复杂度。Docker 可以作为高级用户自行承担边界配置的部署方式，但不作为 MVP 的主要安装与支持路径。
