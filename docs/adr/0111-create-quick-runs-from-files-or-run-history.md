# 从文件或历史 Run 创建快捷执行项

ScriptBoard 允许管理员从受管脚本文件或历史 Run 显式创建快捷执行项，但绝不自动把文件或历史转换为入口。从文件创建时由管理员填写名称、参数模板和超时，`source_run_id` 为空且保存本身不启动脚本；从历史创建时继续保留来源 Run ID。两种来源都只引用受管相对路径，并在每次启动时重新校验文件、变量和执行器。这个决定取代 ADR-0015，使尚未运行的可信脚本也能预先配置为常用入口，同时保留显式创建和可追溯来源的边界。
