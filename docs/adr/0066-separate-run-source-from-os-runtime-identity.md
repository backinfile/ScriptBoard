# 分离执行触发来源与操作系统运行身份

每个 Run 分别保存触发来源和实际运行身份：来源区分 `admin/manual`、`admin/quick-run` 与 `scheduler`，并关联快捷项或计划触发 ID；运行身份记录 Linux 用户名/UID 或 Windows 账号/SID。历史还保存来源对象当时的名称快照，即使快捷项或计划项后来删除也能解释执行。这样避免把唯一 Web 管理员、无人值守调度器和服务系统账号混为一个“执行用户”字段。
