# 只以新提交恢复单个文件

文件详情页可查看单个受保护文件的提交历史，UTF-8 文本展示差异，二进制只展示大小与摘要变化；管理员可将一个历史版本恢复到当前路径，包括恢复已删除文件。恢复前保存当前脏状态，恢复后创建新的 restore 提交，永不改写已有历史。任意 Run 活动时禁用恢复，目标为目录、受限链接或特殊文件时拒绝覆盖；MVP 不提供整仓或目录恢复、branch、merge、rebase、reset 与 cherry-pick。
