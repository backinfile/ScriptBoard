# 限制本地 Git 空间但不自动删除可达历史

`.git/` 默认最大 5 GiB且不能配置为无限，达到 80% 时在网页和托盘警告，达到上限后停止新提交并使版本保护进入异常状态。系统只在无活动 Run 时执行禁用 Hooks 的定期 `git gc`，用于压缩对象和清理安全的不可达临时对象，不重写分支或删除可达提交。仓库占用计入受管卷空间监控，管理员可提高上限，或停用保护后使用系统工具人工归档。
