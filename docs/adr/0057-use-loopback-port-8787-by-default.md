# 默认在本机回环端口 8787 提供 HTTP

ScriptBoard 默认通过 `http://localhost:8787` 访问，并仅监听 `127.0.0.1:8787` 与 `[::1]:8787`；端口可配置，但明文 HTTP 的监听地址不能改为非回环地址。必需监听器端口被占用时服务拒绝启动，不自动改用随机端口。Windows 托盘从最终生效配置解析打开地址，服务日志也明确输出该本机 URL。
