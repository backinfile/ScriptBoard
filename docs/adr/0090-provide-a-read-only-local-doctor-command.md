# 提供只读的本机诊断命令

`scriptboard doctor` 只读检查配置与权限、受管/状态目录边界、SQLite 完整性与 schema/WAL、Run 与日志对应关系、执行器链、端口、TLS 证书与密钥、Windows 服务/托盘或 systemd 注册状态以及磁盘空间。它默认不修复，不输出密码、Session 或变量值；任何会改变状态的未来修复都必须使用独立明确子命令并要求服务停止，以免诊断过程扩大故障。
