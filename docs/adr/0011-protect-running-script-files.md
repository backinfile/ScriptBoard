# 保护运行中的脚本文件

脚本启动请求被接受时取得执行租约，直到该路径的所有活动实例结束前，文件页面禁止覆盖、重命名、移动或删除脚本文件，也禁止重命名、移动或删除从脚本父目录到受管根目录之间的祖先目录。祖先目录内其他普通文件仍可管理。系统不承诺阻止绕过网页的磁盘修改；进程启动前会重新校验脚本并记录内容摘要，以说明实际执行版本。这个折中避免路径操作绕过保护，同时不假装能够推断和冻结脚本的全部依赖。
