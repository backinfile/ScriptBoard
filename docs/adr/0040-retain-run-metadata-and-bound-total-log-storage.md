# 永久保留执行元数据并限制日志总量

Run 元数据默认永久保留，日志文件默认保留 90 天且全局总量最多 1 GiB；任一限制触发时，从最旧的已结束 Run 开始删除日志，将记录标记为 `log_expired`，但不删除执行事实。活动执行和 `disconnected` 执行不参与自动清理。保留天数与总量均可配置但不能同时关闭，清理行为写入应用审计记录，以避免无限历史日志耗尽内部状态磁盘。
