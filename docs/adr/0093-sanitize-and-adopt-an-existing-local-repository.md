# 净化配置后接管已有本地仓库

ScriptBoard 可接管工作树根恰好等于受管根目录的普通非 bare 仓库，保留对象、提交、分支、标签与当前工作树；`.git` 必须是真实本地目录而非链接、挂载点或 gitfile。接管前执行只读完整性检查并展示分支、提交、远程和脏状态，备份原配置后替换为最小本地配置，禁用 Hooks、外部 diff/filter/fsmonitor、credential helper、子模块和所有远程网络操作。所有权不匹配必须二次确认且不得配置全局 `safe.directory=*`，接管全程写入审计记录。
