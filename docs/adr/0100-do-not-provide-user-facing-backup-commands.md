# 不提供面向管理员的备份与恢复命令

ScriptBoard 不提供 `backup create`、`backup restore` 或网页备份功能，受管文件、SQLite 状态和本地 Git 仓库均由管理员使用操作系统或第三方备份工具保护。本地 Git 版本保护只用于恢复误改，不宣称能够防止磁盘损坏或整机丢失。数据库 schema 升级前由应用创建的内部安全快照仍保留，但它仅用于迁移失败回滚，不是通用备份接口。
