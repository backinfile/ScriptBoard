# 网页删除先进入应用内回收站

文件页面的删除操作不立即移除内容，而是将文件或目录移动到受管根目录的保留 `.scriptboard-trash/` 区域，并记录原相对路径、删除时间和大小。该区域从普通文件列表隐藏，仅由专用回收站页面展示，支持无覆盖恢复和人工永久清理，默认不自动过期。运行中脚本及受保护祖先目录仍不能删除。这个保留区域是“受管根目录内全部文件可见”的明确例外，用于降低最高权限文件管理造成的误删风险。
