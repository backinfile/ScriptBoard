# 默认回环并允许配置其他监听地址

ScriptBoard 继续默认监听 `127.0.0.1:8787`，但管理员可通过现有配置层显式绑定任意合法 TCP 地址，非回环明文 HTTP 不再被启动校验拒绝。由于该界面具备主机文件与脚本执行权限，非回环部署仍应优先使用 TLS、可信网络或 HTTPS 反向代理；这项决定取代 ADR-0019、ADR-0082，以及 ADR-0023 和 ADR-0057 中的非回环限制。
