# 外部文件变化只在明确操作边界进入 Git 历史

ScriptBoard 不后台轮询受管目录，也不使用文件系统 watcher。版本保护只在启用/重新启用、网页文件变更、首个 Run 启动前、最后一个 Run 结束后，以及管理员点击“立即创建检查点”时检查并提交外部变化；其余时间外部磁盘修改保持为脏工作树。文件详情可只读显示当前 Git 状态。外部篡改 `.git/`、配置或 HEAD 在下一次 Git 操作时被检测并进入异常状态。
