# 使用主机文件而非运行附件

ScriptBoard 不提供仅属于某次执行的附件上传入口和私有上传目录。管理员或维护员先通过文件页将输入上传到允许写入的主机目录，再以普通绝对路径或相对工作目录的参数交给脚本；执行记录只保存参数文本，不复制输入文件。文件上传与执行仍是两个概念，但历史执行不会冻结输入文件版本，之后修改或删除文件可能使同一参数无法复现原结果。所有路径仍由 [ADR-0122](./0122-browse-the-host-filesystem-with-protected-paths.md) 的主机文件边界统一校验。
