# 使用普通参数变量而非秘密存储

ScriptBoard 允许管理员预存具名文本变量并在执行参数中引用，但所有变量都按普通数据处理：值以明文形式存入 SQLite、对已登录管理员回显，并且不提供加密、遮罩或秘密类型。变量引用必须独占一个参数，不能嵌入其他文本，从而保证变量值不会改变参数数组边界。快捷执行项和计划项保存变量引用并在每次启动时重新解析；Run 同时保存原始模板与当时解析后的实际参数数组，因此修改变量只影响之后的执行。界面必须说明该机制不是凭据保险库，避免用户误以为 Token 或密码受到额外保护。
