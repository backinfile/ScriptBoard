# 使用可撤销的持久服务端管理员会话

ScriptBoard 允许唯一 admin 在多个浏览器同时登录，Session 空闲 12 小时失效且绝对有效期为 7 天。浏览器仅持有随机不可预测 ID，服务端在 SQLite 中保存其哈希，因此会话可单独或批量撤销；修改用户名或密码会撤销全部现有 Session，普通退出只撤销当前 Session，并提供“退出所有设备”。服务重启不自动使有效 Session 失效。
