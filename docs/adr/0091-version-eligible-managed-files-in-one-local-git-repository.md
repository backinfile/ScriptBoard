# 用单一本地 Git 仓库保护符合条件的受管文件

管理员可在网页为受管根目录启用本地 Git 版本保护，不配置或同步远程仓库。仓库默认自动跟踪不超过 10 MiB 的普通文件，阈值可配置但不能无限；更大文件仍可管理但明确标记为未受保护。`.git/`、`.scriptboard-trash/`、上传临时文件、受限链接和挂载点不跟踪，其中 `.git/` 是文件页面不可进入、下载、编辑或删除的应用保留目录。页面展示每个文件是否受版本保护，避免把本地版本历史误解为完整备份。
