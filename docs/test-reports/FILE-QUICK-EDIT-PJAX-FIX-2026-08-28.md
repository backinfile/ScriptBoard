# 文件快捷访问编辑 PJAX 回归修复报告

测试时间：2026-08-28（Asia/Shanghai）  
分支：`dev`

## 结论

- 已修复从侧栏通过 PJAX 进入文件页后，快捷访问条目的编辑按钮无响应的问题。
- 文件页两阶段加载清理时，提升到 `body` 的编辑抽屉会先归还原页面位置；局部数据替换后可再次初始化并提升，整页离开时则随旧页面移除。
- 抽屉仍覆盖完整视口，修改到共享分组后可正常保存。

## 复现与回归

修复前，外部 Chromium 复现脚本连续两次返回失败：快捷条目存在，但点击后抽屉节点不存在。完整浏览器门禁也稳定停在快捷访问编辑入口。

修复后：

1. 外部 Chromium 连续两次执行“登录 → 监控页 → 侧栏进入文件页 → 点击快捷项编辑”，均确认抽屉已打开、父节点为 `body`、输入框获得焦点且浏览器错误为 0。
2. 完整 Chromium desktop gate 通过，覆盖抽屉视口定位、修改共享分组和保存。
3. `go test -p 1 ./... -count=1` 通过。
4. `go vet ./...`、`go build ./cmd/...`、前端语法检查及 `git diff --check` 通过。

## 保留部署

| 项目 | 值 |
| --- | --- |
| URL | `http://127.0.0.1:18869` |
| 进程 | `scriptboard-dev-quick-edit-final.exe`，PID `48284` |
| 监听边界 | 仅 `127.0.0.1:18869` |
| State Root | `.scratch/local-deploy-richdata-final-20260828/state` |
| 登录用户 | `admin` |
| 初始密码 | 仅保留在 State Root 的 `secrets/initial-admin-password`，未写入报告 |
| 二进制 SHA-256 | `CD44D091D2D6C0760A1414BFE8DDD1D099C864FC78C2AC09BB547A677E3085F9` |

最终登录页、监控页和文件页均返回 HTTP 200；保留 2 个文件快捷项、5 个共享分组，标准错误日志为 0 字节。
