# 文档与快捷访问页面本地测试报告

日期：2026-09-05。基线：dev / 2669aa0。

## 本次修改

- 文档列表显示文件系统创建时间，沿用文件页的可用性语义；文件系统不提供创建时间时显示不可用。时间经 Broker 传递，桌面和移动端均可查看。
- 快捷访问使用完整页面的全宽分组列表；保留分组管理、重命名、移除、拖动排序及折叠，并为分组按钮补充可访问名称。
- 文档入口保留来源页及搜索条件，预览、编辑、取消、返回和保存成功后回到文档页。返回地址限制为本地文档路由。

## 验证条目与结果

| 条目 | 结果 |
| --- | --- |
| 修改前文档、快捷访问定向基线 | 通过 |
| 文档编辑来源回归 | 修改前失败，修改后通过 |
| 创建时间经过真实 Broker 后与文件列表一致 | 通过 |
| 全仓包测试 | 初次只有旧快捷访问结构契约失败；更新契约后完整 Web 包重跑通过，其他包在全仓运行中通过 |
| 相关包 go vet | 通过 |
| JavaScript 语法检查 | 通过 |
| 快捷访问延迟加载与分组导航 Chromium 回归 | 通过 |
| 主部署登录、页面加载、四个创建时间 | 通过 |
| 快捷访问重命名、移入分组、折叠 | 通过 |
| 390 / 768 / 1440 像素布局、无横向溢出 | 通过 |
| 文档预览、编辑的返回链接及原搜索条件 | 通过 |
| 隔离文件环境中预览 → 编辑 → 保存 → 返回搜索结果 | 通过，已读取磁盘文件确认内容 |
| 浏览器 pageerror | 无 |
| 布局机械检查 | 无报告项 |

使用外部 Chromium；稳定状态截图已人工查看。主部署测试账号沿用 admin / ScriptBoard-Test-2026!，地址 http://127.0.0.1:19055，原 State Root 与测试数据保留。

主部署服务账号保存主机文件时受现有磁盘回收目录权限限制，接口返回 `protect filesystem trash: read directory owner: Access is denied.`。该环境限制发生在写入前；本次没有修改磁盘权限。完整保存链路另在仓库 fixture 的隔离文件根目录验证，文件操作仍经过真实应用接口。

隔离验证地址：http://127.0.0.1:19056，账号 admin / calibration-ledger-2026；应用监控数据来自 fixture。状态和已保存的 README 保留在用户临时目录 `scriptboard-documents-ui-20260905`。截图与验证脚本归档在主部署目录的 `documents-quick-access-qa` 中。
