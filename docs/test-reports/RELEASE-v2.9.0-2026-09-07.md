# v2.9.0 发布前验证

日期：2026-09-07。发布分支：`release/2.9.0`；正式 Tag：`v2.9.0`。从 `dev` 完成发布准备。

## 本地验证

- 全仓 `go test ./... -count=1`：通过。
- `go vet ./...` 与 `go build ./cmd/...`：通过。
- 横屏样式修复后，`go test ./internal/web -parallel 4 -count=1`：通过（111.115 秒）。
- 外部 Chromium 完整浏览器门禁 `pnpm test`：通过（退出码 0）。
- 外部 Chromium 灵感空间独立用例连续 3 次通过，覆盖笔记、待办、链接、计时、绘图、横屏、持久化和并发冲突。
- 文件跳转独立用例在刻意延迟后退请求时通过，覆盖 24 项行为。
- 外部 Edge 横屏复现：844 × 390 下，绘图区高度从约 18px 恢复为约 32px，原有高度断言通过。
- 重新部署后的管理员登录及 13 项页面、接口、静态资源访问检查通过。
- 前序修复已验证新建空间响应丢失重试，以及跨站 HTTPS iframe 的语言切换、登录与刷新保留；见 [审查修复报告](./RELEASE-REVIEW-FIXES-2026-09-07.md)。

## 发布前发现与处理

- 修复短屏模块标题输入框继承全局最小高度、挤占超大画板绘图区的问题。短屏标题框最小高度设为 28px，与现有紧凑布局一致。
- 计时检查使用最多 5 秒的实际显示变化等待，保留计时必须前进的断言。
- 文件跳转后退检查等待目标目录入口完成恢复，再操作跳转对话框；通过延迟后退请求确定性复现并验证。
- 本次测试生成的截图保存在本地验证目录，不替换仓库中的既有截图。

## 保留本地部署

- 地址：http://127.0.0.1:29890。
- 用户名：`admin`；测试密码：`Review fixes local password 2026!`。
- 进程：63432，以 `.scratch/release-v290/pid.txt` 为准。
- 程序、独立状态、日志、脚本、截图及结果：`D:\Github\ScriptBoard\.scratch\release-v290`。
- 测试创建的灵感空间、笔记、待办、链接、计时器及绘图数据保留。

## 云端发布

正式产物由 `.github/workflows/release.yml` 在目标 Tag 上重新通过 Windows 测试/vet/构建/SCM、race、fuzz、浏览器、安全扫描及 CodeQL 后生成，随后执行签名、provenance attestation、Linux 冒烟检查和发布。四个平台安装器与验证文件的最终结果以该次 GitHub Actions 为准。

本机验证不替代云端门禁；本次未新增双 systemd 环境的安装/回滚演练或真实上一版本受管主机的在线升级演练。
