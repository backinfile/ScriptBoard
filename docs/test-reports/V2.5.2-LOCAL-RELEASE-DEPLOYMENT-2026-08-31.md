# ScriptBoard v2.5.2 本地发布候选部署测试报告

测试时间：2026-08-31（Asia/Shanghai）  
测试分支：`dev`  
被测提交：`2033e9578208db77e2965c4bcb460a87ad4290a2`

## 结论

- `v2.5.1` 正式 workflow 的 Windows、Chromium、fuzz、security 与 CodeQL 均通过，最终仅 race 中 `TestSingleActiveRunUsesDirectShellDestination` 偶发 `SQLITE_BUSY`；安装包与 GitHub Release 未创建。
- race 日志确认测试夹具另开实时 SQLite 连接时未配置项目统一的 busy timeout。现已让该文件的 3 个外部夹具连接使用 `busy_timeout(5000)` 和单连接配置。
- 两个共享数据库的相关契约测试在高并发配置下连续重复 100 次通过；定向前端契约通过。
- `v2.5.2` 候选已重新构建并部署到保留的丰富测试 State Root；6 组黑盒验收全部通过，浏览器 console error 和 page error 均为 0。

## 本地部署

| 项目 | 值 |
| --- | --- |
| URL | `http://127.0.0.1:18869` |
| 进程 | `scriptboard-v2.5.2-rc.exe`，PID `40088` |
| 监听边界 | 仅 `127.0.0.1:18869` |
| State Root | `.scratch/local-deploy-richdata-final-20260828/state` |
| 登录用户 | `admin` |
| 登录密码 | 通过 `scriptboard admin reset` 获取的一次性密码，仅短暂保存在 State Root 私有文件中；登录后已由应用删除 |
| 二进制 SHA-256 | `EE82B6C4A9A1EB0CE9214B58C0C44A4C0C6C4B9F1187E410C2589CBC6EC268D7` |
| 结构化结果 | `.scratch/local-release-v252-20260831/blackbox-results.json` |
| 截图 | `.scratch/local-release-v252-20260831/dashboard-monitor.png`、`dashboard-public.png` |
| 标准错误日志 | 0 字节 |

部署、State Root、结构化结果、截图和原有丰富测试数据均保留。

## race 回归闭环

| 编号 | 测试条目 | 结果 |
| --- | --- | --- |
| R01 | GitHub `release-race`：`TestSingleActiveRunUsesDirectShellDestination` | 修复前偶发 `SQLITE_BUSY` |
| R02 | 前端契约外部 SQLite 连接使用 5 秒 busy timeout 与单连接 | 已修复 |
| R03 | `TestApplicationShellAndStatusEndpointShareFiveSecondSnapshot` 与 `TestSingleActiveRunUsesDirectShellDestination`，`-count=100 -parallel=100` | 通过 |

## 黑盒测试条目

| 编号 | 测试条目 | 结果 |
| --- | --- | --- |
| B01 | 登录页 200、匿名受保护页面 303、未知路径 404、CSP 与 `nosniff` | 通过 |
| B02 | 使用重置后的一次性管理员密码真实登录并进入 `/monitor` | 通过 |
| B03 | 快捷执行、计划、变量、文件、网站监控和数据库页面全部返回 200 | 通过 |
| B04 | 仪表盘管理页保持浅色作用域，监控页显示 5 张深色渐变卡片 | 通过 |
| B05 | 匿名公开仪表盘返回 200；渐变 SVG 无内联样式，卡片实体底色正确 | 通过 |
| B06 | 保留的快捷访问和 MySQL 测试数据可见；浏览器 console error / page error 为 0 | 通过 |

黑盒统计：**6 项通过、0 项失败**。

## 既有完整门禁

本次只修改测试夹具。相同产品代码在 `v2.5.1` 正式 workflow 中已通过 Windows、Chromium、fuzz、security 和 CodeQL，在 `dev` 预检中也通过 Linux race；`v2.5.2` 提交继续执行全仓 Go 测试、Vet、构建与模块校验，最终以正式 Tag workflow 结果为发布依据。
