# 已退役功能清理本地部署测试报告

测试日期：2026-08-25

测试提交：`e78a2c4`

测试环境：Windows amd64，本地便携部署

## 结论

通过。代码、配置、发布定义、数据库迁移、页面入口和活动文档中的已退役功能均已清理；当前产品按 Web、Broker、Runner 三组件边界构建。

## 自动化验证

| 项目 | 结果 |
| --- | --- |
| `go test ./...` | 通过 |
| `go vet ./...` | 通过 |
| Linux amd64 核心命令构建 | 通过 |
| Chromium 桌面门禁 | 通过 |
| 全仓代码、路径、配置与文档关键词复扫 | 通过，零命中 |
| 合并最新 `dev` 后的 Web、迁移与自定义面板测试 | 通过 |

## 本地部署

| 项目 | 结果 |
| --- | --- |
| 地址 | `http://127.0.0.1:18842` |
| 进程 ID | `42360` |
| 部署目录 | `.scratch/local-deploy-feature-cleanup-e78a2c4` |
| 登录用户 | `admin` |
| 初始凭据 | 保存在部署 State Root 的 `secrets/initial-admin-password` |
| 登录页 | HTTP 200 |
| 标准错误日志 | 0 字节 |
| 部署文件 | 主程序及标准输出、标准错误日志；无额外宿主程序 |

部署在报告生成后保持运行，测试数据与初始凭据文件保留。

## HTTP 验收

共执行 13 项，全部通过：

- 登录页、认证后首页、账户设置、服务日志、数据库工作区、快捷执行和更新页均返回 HTTP 200。
- 五个已退役页面或接口返回 HTTP 404。
- 已退役静态资源返回 HTTP 404。
- 主要页面未渲染已退役入口、配置项或服务筛选项。
