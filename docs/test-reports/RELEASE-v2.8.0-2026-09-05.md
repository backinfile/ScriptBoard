# v2.8.0 发布前验证

日期：2026-09-05。产品源码基线：dev / e33514e。

## 检查清单与结果

- 全仓 `go test ./... -count=1`：通过。
- `go vet ./...` 与 `go build ./cmd/...`：通过。
- 外部 Chromium 完整浏览器发布检查：通过（pnpm test，退出码 0）。
- HTTP 基础检查：登录页、管理员登录、监控、文件、文档、快捷访问、快捷执行、计划、运行历史、Kubernetes、CSS、JavaScript、快捷访问接口，共 13 项通过。
- 测试数据创建：26 次请求通过，保留 10 个示例文件、4 条文档收藏、4 个目录快捷访问、3 个快捷执行和 2 个暂停计划。
- 3 个示例脚本均执行成功；2 个计划确认暂停。

## 保留的本地部署

地址：http://127.0.0.1:19058。用户名：admin。测试密码：ScriptBoard-Test-2026!。

状态目录、日志、验证脚本和结果：`.scratch/local-release-v280-20260905/`。程序：`.scratch/local-dev-20260905/scriptboard-v280-test.exe`。进程编号以部署目录 `pid.txt` 为准。

原 19055 部署的状态目录无法由本次执行身份访问，因此本次使用独立状态目录启动；原部署仍在运行。未连接真实 Kubernetes、MySQL 或 Redis 服务，相关行为由全仓测试覆盖。

## 发布检查维护

浏览器检查随快捷访问独立页面更新：覆盖文件页固定目录、独立页分组编辑、刷新持久化、目录跳转和取消固定，并同步移动端文件页按钮数量。产品源码未变更。

本地 Go 测试首次因沙箱无法访问缓存及测试目录失败；使用获准的本机执行权限后全仓通过。

云端 CI 与 release workflow 的结果以 GitHub Actions 为准；正式发布前还需通过 Windows SCM、Linux、race、fuzz、安全扫描、签名构建及安装包验证。

