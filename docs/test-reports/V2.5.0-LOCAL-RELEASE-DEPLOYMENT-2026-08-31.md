# ScriptBoard v2.5.0 本地发布候选部署测试报告

测试时间：2026-08-31（Asia/Shanghai）  
测试分支：`dev`  
被测提交：`ec4dada77f95797bccb59dd7f299b1b8917a525a`

## 结论

- 全仓 Go 测试、Vet、命令构建、模块校验和完整外部 Chromium Gate 全部通过。
- 五组正式发布 fuzz 安全边界均以 30 秒配置通过。
- 当前候选二进制已替换上一轮本地部署，并继续使用保留的丰富测试 State Root；6 组黑盒验收全部通过，浏览器 console error 和 page error 均为 0。
- 仪表盘 CSP 阻塞已修复：SVG 渐变定义不再使用内联样式，渐变卡片同时保留可计算的实体背景色。
- Windows SCM 和 race 门禁依赖提权 Windows 与启用 CGO 的 Linux 环境，本机会话不能执行；正式 Tag workflow 会在对应 GitHub runner 上强制执行，全部通过后才发布安装包。

## 本地部署

| 项目 | 值 |
| --- | --- |
| URL | `http://127.0.0.1:18869` |
| 进程 | `scriptboard-v2.5.0-rc.exe`，PID `56792` |
| 监听边界 | 仅 `127.0.0.1:18869` |
| State Root | `.scratch/local-deploy-richdata-final-20260828/state` |
| 登录用户 | `admin` |
| 登录密码 | 通过 `scriptboard admin reset` 获取的一次性密码，仅短暂保存在 State Root 私有文件中；登录后已由应用删除 |
| 二进制 SHA-256 | `03A2349FF57478DFBD3D891438C8178A3499EFCA11ECE3C4369AE3DA4887508C` |
| 结构化结果 | `.scratch/local-release-v250-20260831/blackbox-results.json` |
| 截图 | `.scratch/local-release-v250-20260831/dashboard-monitor.png`、`dashboard-public.png` |
| 标准错误日志 | 0 字节 |

部署、State Root、测试脚本、结构化结果、截图和原有丰富测试数据均保留。

## 黑盒测试条目

| 编号 | 测试条目 | 结果 |
| --- | --- | --- |
| B01 | 登录页 200、匿名受保护页面 303、未知路径 404、CSP 与 `nosniff` | 通过 |
| B02 | 使用重置后的一次性管理员密码真实登录并进入 `/monitor` | 通过 |
| B03 | 快捷执行、计划、变量、文件、网站监控和数据库页面全部返回 200 | 通过 |
| B04 | 仪表盘管理页保持浅色作用域，监控页显示 5 张深色渐变卡片 | 通过 |
| B05 | 匿名公开仪表盘返回 200；渐变 SVG 无内联样式，卡片实体底色为 `rgb(16, 19, 32)` | 通过 |
| B06 | 保留的快捷访问和 MySQL 测试数据可见；浏览器 console error / page error 为 0 | 通过 |

黑盒统计：**6 项通过、0 项失败**。

## 白盒与安全门禁

| 编号 | 命令或范围 | 结果 |
| --- | --- | --- |
| W01 | `go test -p 1 ./... -count=1` | 通过 |
| W02 | `go vet ./...` | 通过 |
| W03 | `go build ./cmd/...` | 通过 |
| W04 | `go mod verify` | 通过 |
| W05 | `pnpm test`（`integration/browser`，外部 Playwright Chromium） | 通过 |
| W06 | `FuzzPolicyAllowsAddress`，30 秒 | 通过 |
| W07 | `FuzzNormalizeHTTPHost`，30 秒 | 通过 |
| W08 | `FuzzValidRequestTarget`，30 秒 | 通过 |
| W09 | `FuzzSafeArchivePath`，30 秒 | 通过 |
| W10 | `FuzzParseArgumentsRejectsControlCharacters`，30 秒 | 通过 |

## 本机环境限制

- `scripts/windows-scm-security-gate.ps1` 正确拒绝未提升的当前 Windows 会话；正式 workflow 使用具备相应能力的 `windows-latest` runner。
- 本机 Go 的 `CGO_ENABLED=0`，无法执行 `go test -race`；正式 workflow 在 Ubuntu 上执行同一 race 包集合。
- Git Bash 未继承 Windows Go 路径且对 CRLF fuzz 脚本解释异常，因此本机改用相同的五条原生 `go test -fuzz` 命令完成等价验证。
