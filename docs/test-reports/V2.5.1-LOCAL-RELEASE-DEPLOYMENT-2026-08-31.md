# ScriptBoard v2.5.1 本地发布候选部署测试报告

测试时间：2026-08-31（Asia/Shanghai）  
测试分支：`dev`  
被测提交：`8191bb8634f6a2a831a64e88ddbbd087a82204b4`

## 结论

- `v2.5.0` 正式 workflow 在安装包阶段前被浏览器与 fuzz 门禁阻断，没有创建 GitHub Release 或发布安装包；原 release 分支和 Tag 保持不变。
- 已在 `dev` 修复 deferred PJAX 快捷访问竞态，并把请求目标 fuzz 范围对齐服务器真实的 1 MiB HTTP 请求头边界。
- 新增的延迟响应浏览器契约在修复前稳定失败，修复后连续 10 次通过；完整外部 Chromium Gate 随后通过。
- 全仓 Go 测试、Vet、命令构建、模块校验和五组 30 秒 fuzz 全部通过。
- `v2.5.1` 候选已重新部署到保留的丰富测试 State Root；6 组黑盒验收全部通过，浏览器 console error 和 page error 均为 0。

## 本地部署

| 项目 | 值 |
| --- | --- |
| URL | `http://127.0.0.1:18869` |
| 进程 | `scriptboard-v2.5.1-rc.exe`，PID `40020` |
| 监听边界 | 仅 `127.0.0.1:18869` |
| State Root | `.scratch/local-deploy-richdata-final-20260828/state` |
| 登录用户 | `admin` |
| 登录密码 | 通过 `scriptboard admin reset` 获取的一次性密码，仅短暂保存在 State Root 私有文件中；登录后已由应用删除 |
| 二进制 SHA-256 | `72C5A15F27D43DD6AC2CF50FB01DB64BA749E8805DBEFB36ACBCABCA209793AC` |
| 结构化结果 | `.scratch/local-release-v251-20260831/blackbox-results.json` |
| 截图 | `.scratch/local-release-v251-20260831/dashboard-monitor.png`、`dashboard-public.png` |
| 标准错误日志 | 0 字节 |

部署、State Root、测试脚本、结构化结果、截图和原有丰富测试数据均保留。

## 回归闭环

| 编号 | 测试条目 | 结果 |
| --- | --- | --- |
| R01 | 延迟 350 ms 返回文件页第二阶段数据，在 shell 阶段打开快捷访问编辑抽屉 | 修复前稳定失败 |
| R02 | shell 阶段跳过快捷访问初始化，最终数据阶段再启用 | 修复后连续 10 次通过 |
| R03 | `FuzzValidRequestTarget` 对齐 `http.Server.MaxHeaderBytes` 的 1 MiB 边界 | 30 秒通过 |

## 黑盒测试条目

| 编号 | 测试条目 | 结果 |
| --- | --- | --- |
| B01 | 登录页 200、匿名受保护页面 303、未知路径 404、CSP 与 `nosniff` | 通过 |
| B02 | 使用重置后的一次性管理员密码真实登录并进入 `/monitor` | 通过 |
| B03 | 快捷执行、计划、变量、文件、网站监控和数据库页面全部返回 200 | 通过 |
| B04 | 仪表盘管理页保持浅色作用域，监控页显示 5 张深色渐变卡片 | 通过 |
| B05 | 匿名公开仪表盘返回 200；渐变 SVG 无内联样式，卡片实体底色为 `rgb(16, 19, 32)` | 通过 |
| B06 | 保留的快捷访问和 MySQL 测试数据可见；浏览器 console error / page error 为 0 | 通过 |

黑盒统计：**6 项通过、0 项失败**。

## 白盒与安全门禁

| 编号 | 命令或范围 | 结果 |
| --- | --- | --- |
| W01 | `go test -p 1 ./... -count=1` | 通过 |
| W02 | `go vet ./...` | 通过 |
| W03 | `go build ./cmd/...` | 通过 |
| W04 | `go mod verify` | 通过 |
| W05 | `pnpm test`（含 delayed-PJAX 回归契约和外部 Playwright Chromium） | 通过 |
| W06 | 五组正式发布 fuzz，每组 30 秒 | 通过 |

Windows SCM 与 race 仍由正式 Tag workflow 在提权 Windows 和启用 CGO 的 Linux runner 上执行。
