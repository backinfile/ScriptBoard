# 调度与 Redis 审查问题修复验证

日期：2026-09-05。修复起点：dev / 42580c1。平台：Windows amd64，Go 1.26.6，Node.js 24.14.0。

## 修改与回归

| 条目 | 结果 |
| --- | --- |
| 计划的禁止重叠策略 | 两个手动请求只有一个成功；自动调度与手动启动竞争时自动触发记录 skipped |
| MySQL 不存在的备份日期 | 创建与编辑均拒绝 2 月 31 日；启动协调与轮询停用历史无效计划，不创建备份操作 |
| MySQL 备份计划推进 | 下一次执行时间保存失败时不备份；到期状态通过条件更新领取 |
| Redis IPv6 | 使用正确的主机端口拼接，IPv4/IPv6 均通过真实 TCP/TLS 握手与 Redis PING 测试 |
| Redis 连接模式 | 明文、验证证书 TLS、显式跳过证书验证 TLS 均通过；不改写用户选择 |
| Redis 空密码编辑 | 明确勾选“使用空密码”可修改连接参数；未明确提供凭据时拒绝端点重绑定；冲突输入被拒绝 |
| Redis HTTP 表单 | 空密码选项从表单传到领域层，支持三种连接模式 |

四个原始回归用例在修复前运行失败，在修复后通过。新增用例使用模拟进程、本地 RESP 服务及临时 SQLite 数据，不依赖外部数据库。

## 自动验证

- go test ./... -count=1：通过，63 个包通过、13 个包无测试。
- go vet ./...：通过。
- go build ./cmd/...：通过。
- 14 个 integration/browser/*-contract.cjs：通过。
- 新增 Redis 实际连接矩阵：IPv4/IPv6 × 三种连接模式，6 项通过。
- git diff --check：通过。
- 未执行 Linux 专用运行测试、race、长时间 fuzz、完整 run.cjs 浏览器门禁或生产服务安装；本机 CGO_ENABLED=0。

## 重新本地部署

从本次 worktree 构建 integration/browser/fixture，启动实际 Web 应用。二进制与测试数据目录分离；使用独立本地端口，不替换既有部署。测试主机监控来自 fixture，文件、调度、身份和数据库连接配置经过实际应用路径。

访问地址：http://127.0.0.1:13731 。测试用户名：admin；密码使用 fixture 已有的 calibration-ledger-2026。

按以下条目执行真实 HTTP 检查，共 23 项通过：匿名访问重定向、登录页与登录、监控/文件/计划/数据库/运行历史基础访问、创建 IPv6 无密码连接、空密码控件存在、拒绝隐式凭据重绑定、三种模式的显式修改与选择保留、创建 MySQL 连接元数据、拒绝并解释无效备份日期、创建禁止重叠计划、两个并发请求返回 303 与 409、运行详情可访问。

外部 Chromium 检查：1440×1000 桌面及 390×844 移动端，空密码复选框可见且可操作，密码输入保持空白，无横向溢出，无 pageerror。截图已人工查看。

测试部署与测试数据保留在本机临时目录 scriptboard-review-25945606649945e18fdb3cf07b42af12，二进制在 bin，数据在 data；验证日志与截图归档到 artifacts。没有连接真实 MySQL，也没有执行生产数据库变更。HTTP 连接配置指向未运行的 Redis 测试端口，因此概览可显示连接不可用；实际 TCP/TLS 成功路径由上述本地 RESP 测试覆盖。
