# ScriptBoard v2.5.3 本地发布候选部署测试报告

测试时间：2026-08-31（Asia/Shanghai）  
测试分支：`dev`  
被测提交：`0eba41eb1c656ecb2b25be008b560d3ab979a1ac`

## 结论

- `v2.5.2` 正式 workflow 的 security、CodeQL 等门禁通过，浏览器门禁仅在 deferred PJAX 外壳标题的固定 500ms 等待处超时；安装包与 GitHub Release 未创建。
- 日志和源码确认重数据请求仍被测试门控，失败来自繁忙 runner 超过任意渲染时限，而非产品断言或页面错误。
- 回归改为等待测试已门控的重数据请求信号，再验证外壳标题、说明和 loading region；它保持重数据响应暂停，因此仍能验证两阶段渲染契约。
- 定向导航回归连续 10 次通过，完整 Chromium 浏览器门禁通过。
- `v2.5.3` 候选已重新构建并部署到保留的丰富测试 State Root；6 组黑盒验收全部通过，浏览器 console error 和 page error 均为 0。

## 本地部署

| 项目 | 值 |
| --- | --- |
| URL | `http://127.0.0.1:18869` |
| 进程 | `scriptboard-v2.5.3-rc.exe`，PID `45868` |
| 监听边界 | 仅 `127.0.0.1:18869` |
| State Root | `.scratch/local-deploy-richdata-final-20260828/state` |
| 登录用户 | `admin` |
| 登录密码 | 通过 `scriptboard admin reset` 获取的一次性密码，仅短暂保存在 State Root 私有文件中；登录后已由应用删除 |
| 二进制 SHA-256 | `B64D317676179A942E73C3FDF401FDE5E7A473D4FBDB64730B1DDBB2E87B6F54` |
| 结构化结果 | `.scratch/local-release-v253-20260831/blackbox-results.json` |
| 截图 | `.scratch/local-release-v253-20260831/dashboard-monitor.png`、`dashboard-public.png` |
| 标准错误日志 | 0 字节 |

部署、State Root、结构化结果、截图和原有丰富测试数据均保留。

## 浏览器回归闭环

| 编号 | 测试条目 | 结果 |
| --- | --- | --- |
| R01 | `v2.5.2` release-browser：deferred Variables 外壳标题固定 500ms 等待 | 修复前超时 |
| R02 | 以已门控的数据请求作为外壳提交信号 | 已修复 |
| R03 | `SCRIPTBOARD_BROWSER_SCOPE=navigation`，连续 10 次 | 通过 |
| R04 | 完整 `pnpm --dir integration/browser test` | 通过 |

## 黑盒测试条目

| 编号 | 测试条目 | 结果 |
| --- | --- | --- |
| B01 | 登录页 200、匿名受保护页面 303、未知路径 404、CSP 与 `nosniff` | 通过 |
| B02 | 使用重置后的一次性管理员密码真实登录并进入 `/monitor` | 通过 |
| B03 | 快捷执行、计划、变量、文件、网站监控和数据库页面全部返回 200 | 通过 |
| B04 | 仪表盘管理页保持浅色作用域，监控页显示 5 张深色渐变卡片 | 通过 |
| B05 | 匿名公开仪表盘返回 200；渐变 SVG 无内联样式，卡片实体底色正确 | 通过 |
| B06 | 保留的快捷访问和 MySQL 测试数据可见；浏览器 console error / page error 为 0 | 通过 |

黑盒统计：**6 项通过、0 项失败**。

## 发布门禁

本地全仓 `go test -p 1 ./...`、`go vet ./...`、`go build ./...` 和 `go mod verify` 全部通过。`dev` 预检通过后才创建不可修改的 `release/2.5.3` 与 `v2.5.3`，最终以正式 Tag workflow 和 GitHub Release 结果为发布依据。
