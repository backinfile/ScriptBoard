# 当前 dev 本地部署与测试数据报告

日期：2026-09-05。源码：dev / 2669aa0。使用实际 cmd/scriptboard 构建，通过 HTTP 接口验证。

## 保留的部署

- 地址：http://127.0.0.1:19055
- 用户名：admin
- 测试密码：ScriptBoard-Test-2026!
- PID：61588（重启后以部署目录 pid.txt 为准）
- 程序、State Root 和日志：`.scratch/local-dev-20260905/`
- 示例文件：`.scratch/sample-data-20260905/`
- 填充脚本：部署目录 `seed.ps1`；该脚本不是幂等脚本，重复运行会新增任务。
- 验证脚本及结果：`verify.ps1`、`test-results.json`、`pins.json`、运行详情 HTML。

## 测试数据

10 个文件：4 个文档（Markdown、JSON、TXT）、2 个数据文件（CSV、JSON）、1 个日志、3 个 PowerShell 示例脚本。

已保存 4 条文档收藏、4 个目录快捷访问、3 个快捷执行、2 个共享分组和 2 个计划。计划均已暂停，可手动启用；脚本仅输出示例信息或读取主机信息。

## 检查结果

| 检查项 | 结果 |
| --- | --- |
| 当前 dev 构建 | 通过 |
| 登录页与管理员登录 | 通过 |
| 主机监控、文件、文档、快捷访问、快捷执行、计划、运行历史、Kubernetes 页面 | HTTP 200 |
| CSS、JavaScript 静态资源 | HTTP 200 |
| 4 条文档收藏创建及页面内容验证 | 通过 |
| 4 个快捷访问入口创建及接口读取 | 通过 |
| 3 个快捷执行与 2 个计划创建 | 通过 |
| 重启后登录与数据读取 | 通过，数据保留 |
| 3 个示例脚本执行 | 全部 succeeded，退出码 0 |
| 2 个计划暂停 | 通过 |
| 服务 stderr | 0 字节 |

填充阶段记录了 26 次成功 HTTP 请求，另行检查登录、文档内容、计划暂停、重启持久化和运行日志。

首次执行的 3 条记录因 Windows PowerShell 执行策略返回失败，已保留供查看。通过仅在测试服务进程环境设置 `PSExecutionPolicyPreference=Bypass` 并重启后，3 个脚本均成功。未修改机器或用户全局执行策略。

未连接真实 Kubernetes、MySQL、Redis 服务；对应集群、备份与连接行为不属于本次实际验证范围。本次未修改产品源码，也未重跑全仓测试。

## 自 v2.7.0 以来的变更

比较本地 v2.7.0 标签（2026-09-03，c51efdb）与当前 dev：53 个文件，新增 2446 行、删除 137 行。

- 新增资源文档管理：收藏主机文件，支持分组、排序、搜索、预览和编辑入口。
- 快捷访问迁移到独立配置页，保留文件页固定入口。
- Kubernetes 展示 NodePort、LoadBalancer 与 Ingress 外部入口；修复工作负载成员实时刷新。
- 手动检查更新刷新发布说明，绕过缓存校验。
- 手动和自动计划执行统一落实禁止重叠设置。
- MySQL 备份计划校验不可发生的日期，并防止并发重复领取同一轮任务。
- Redis 支持显式空密码修改连接配置，并修复 IPv6 地址拼接。
- 增补回归测试、数据模型说明和本地测试报告。

另有未提交的 Kubernetes 附加组件管理方案与预览文件，不计入上述已提交变更。远端 GitHub Release 未核实，比较基准使用本地最新版本标签。
