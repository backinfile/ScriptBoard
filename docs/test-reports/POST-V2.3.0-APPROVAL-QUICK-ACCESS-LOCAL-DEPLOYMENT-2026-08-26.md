# v2.3.0 后功能变更与本地部署测试报告

测试时间：2026-08-26（Asia/Shanghai）

测试分支：`codex/quick-run-inline-sort`

对比基线：`v2.3.0`

结论：**通过**。当前分支相对 `v2.3.0` 的功能变更已分类盘点；全仓 Go 白盒测试和仓库 Chromium 浏览器回归门禁通过，外部 Chrome Playwright/HTTP 主流程黑盒验证 9/9 场景组通过，审批详情追加验证 7/7 通过，批量上传同步与运行详情追加验证 7/7 通过，浏览器控制台错误为 0。隔离部署、State Root、测试数据、JSON 结果与截图均保留。

## 1. 自上个 Release 以来涉及的功能

以下范围为 `v2.3.0..72e3be3` 的已提交功能，加上本分支尚未合并的实现。

### 1.1 数据库工作区

1. MySQL 与 Redis 统一为同一套连接工作区、详情导航和页面结构。
2. MySQL/Redis 页签可在连接类型间混合展示，局部导航只替换所属工作区。
3. MySQL 新增查询工作台；只读查询与写模式分离，写模式通过近期认证确认。
4. 查询行数、超时等限制移入设置抽屉。
5. Redis 新增键浏览器，双冒号命名空间按层级分组。
6. 本地连接验证、安全连接、明文连接及显式跳过证书验证的行为得到加固。
7. 数据库详情面板、空状态、页签高度和抽屉层级完成统一与稳定化。

### 1.2 快捷执行

1. 快捷执行支持拖动排序，排序范围保持在各自分组内。
2. 页面只保留一个“调整顺序”按钮，并固定在“新建分组”左侧。
3. 点击后在当前页面原地进入拖动状态，不进行整页刷新；所有分组同时展开并可排序。
4. 支持取消恢复原顺序、完成后原地保存，以及聚焦卡片后使用方向键调整。
5. 删除了每个分组上重复的排序按钮。
6. 修复多文件上传覆盖脚本时“同步到快捷执行”未生效的问题；文件替换、回收站记录和快捷执行摘要/版本更新现在在同一事务中完成，失败时整个批次回滚。

### 1.3 运行详情

1. “详情”与“参数与技术信息”合并为同一信息区，并移动到日志上方。
2. 来自快捷执行的运行使用快捷执行名称作为页面标题，不再显示“保存为快捷执行”。
3. 新增“重新执行”按钮；快捷执行来源按当前快捷执行配置重新启动，普通脚本运行沿用已保存的脚本、参数与超时。

### 1.4 外部接口与审批

1. 每个日志、上传、变量和快捷执行调用功能新增“需要审批”，默认关闭。
2. 新增“审批”页签，展示调用时间、Key、功能、动作类型、目标、输入摘要，以及上传文件名、大小和 SHA-256。
3. 开启审批后，调用返回 `202 pending_approval`；批准前不执行目标动作。
4. 上传在调用时写入私有缓存，批准时复核实际大小与 SHA-256 后再写入目标文件系统。
5. 批准通过原子领取保证只能执行一次；重复批准返回冲突。
6. 拒绝会删除上传缓存且不写入目标；配置变化、目标失效或执行失败进入失败状态。
7. 服务在审批执行中重启时，审批和调用记录统一恢复为 `failed/unknown`，处理中及孤立缓存被删除，避免不确定动作被重放。
8. 受管 Host Files 后端执行已批准日志时保留原配置的轮转大小与备份数量。
9. 未开启审批的外部上传直接通过受限 Host Files 批次边界写入目标。
10. 旧上传收件箱的包、模板、路由、授权规则、文件页入口和遗留暂存目录全部删除。
11. 每条待审批记录新增仅图标的查看入口，桌面端在当前审批页打开右侧抽屉，移动端使用完整页面回退。
12. 变量详情显示当前值与请求值；日志详情显示待写内容；快捷执行详情显示名称、脚本、参数、超时和修订。
13. 上传详情只读预览对应私有缓存的前 64 KiB；文本直接显示，二进制显示十六进制，并展示目标目录、同名策略及新建、拒绝或自动重命名的实际结果。
14. 待审批上传支持直接下载完整缓存文件；响应禁止缓存，下载不会领取审批、减少待审批数量或写入目标目录。

### 1.5 文件快捷访问

1. 文件行“…”菜单新增 Pin 操作；目录继续支持固定。
2. 固定文件时保存文件类型，点击后进入父目录并聚焦、滚动到该文件行。
3. 每个快捷访问条目的取消固定按钮左侧新增仅图标的编辑按钮。
4. 编辑按钮打开侧边抽屉，可修改独立显示名称，最长 128 个 Unicode 字符。
5. 条目支持鼠标拖动排序，也支持在拖动手柄上使用上下方向键排序；顺序实例级持久化。
6. 文件与目录共用最多 30 项的实例级列表；旧目录记录迁移后保持目录类型。

### 1.6 自定义页面、面板与界面清理

1. 新增可配置自定义页签，包含角色可见性、来源边界、加载时机与抽屉管理。
2. 自定义仪表盘可选择发布为顶层页签；既有面板默认不自动发布。
3. 修复自定义面板重复排序号、页签和抽屉层级问题。
4. 移除 AI Assistant 运行时、页面、Broker 能力、发布产物和残留自动化集成。
5. 统一主页面标题、导入/导出图标、操作菜单层级和部分运营控件。
6. 简化登录页文案，恢复文本预览对比度，稳定数据库与旧 Assistant 区域的纵向布局。
7. 外部日志文件列表的列宽、目标信息与操作区完成对齐。

## 2. 白盒测试条目

### 2.1 快捷执行

1. 单一顶层排序按钮存在且位于“新建分组”左侧。
2. 分组内不再渲染旧排序按钮或旧移动操作。
3. 无 JavaScript 回退模式可由 `?reorder=1` 进入排序状态。
4. 前端点击拦截默认导航并原地切换状态。
5. 所有分组展开、折叠按钮暂时禁用、卡片变为 draggable/focusable。
6. 拖动与键盘排序生成完整分组顺序并通过 CSRF 接口保存。
7. 取消恢复初始 DOM 顺序；保存成功后原地退出；失败恢复并报告错误。
8. 多文件覆盖上传读取 `sync_quick_runs`，本地和 Broker 两条路径均在单一数据库事务中更新回收站记录与快捷执行摘要/版本。
9. 批次元数据事务失败时回滚整个文件批次，避免脚本内容与快捷执行版本不一致。

### 2.2 运行详情

1. 合并后的详情区包含来源、发起人、运行身份、超时、参数、执行器与 SHA-256，并出现在日志区之前。
2. 快捷执行来源使用当前快捷执行名称作为标题，隐藏“保存为快捷执行”。
3. 快捷执行来源的“重新执行”提交到对应快捷执行启动端点；普通脚本来源提交保存的脚本、参数和超时。
4. 删除旧的技术信息折叠区，保留长参数与摘要的可换行显示。

### 2.3 审批领域与缓存

1. 新 Entry 的审批要求默认关闭，更新后可持久化开启状态。
2. 审批保存经过验证的调用输入、Entry 修订快照、来源、上传元数据和消息摘要。
3. `pending → processing` 为原子领取，同一审批只能领取一次。
4. 只允许 `approved/rejected/failed` 三种终态完成处理中的审批。
5. 启动恢复将处理中的审批及对应调用记录标记为未知失败。
6. 上传缓存限制 ID、最大字节数、私有目录和 0600 payload，并计算实际 SHA-256/大小。
7. 缓存只能领取一次；完成、拒绝、处理中断和孤立数据均清理。
8. 变量审批批准前值不变，批准后按变量类型再次解析并递增修订。
9. 日志审批批准前文件不存在，批准后写入；受管模式保留轮转配置。
10. 上传审批批准前目标不存在，批准后内容一致；拒绝时目标不变且缓存为空。
11. 快捷执行审批批准前无 Run，批准后只创建一个 Run。
12. 全局开关、Key、分组、Entry、审批开关、动作类型和 Entry 修订在批准时再次校验。
13. 批准与拒绝都校验会话权限和 CSRF，并写入审批、调用与审计状态。
14. 默认不审批的四类动作继续走即时执行路径。
15. 旧 `/resources/inbox` 路由及子路由返回 404。
16. 审批详情路由只允许具备执行管理权限的会话读取，查看不会领取审批或移动缓存。
17. 上传缓存预览限制审批 ID 格式和读取上限；路径穿越被拒绝，预览后 payload 仍只能领取一次。
18. 上传目标状态使用 Host Filesystem 边界实时检查；同名 `rename` 显示实际可用名称且不改写既有文件。
19. 四种动作均渲染独立语义区域和可访问的数据契约，列表查看按钮具有本地化 accessible name。
20. 抽屉保留批准与拒绝动作、配置修订一致性提示，以及无 JavaScript 的完整详情页能力。
21. 缓存下载只允许具备执行管理权限的会话读取待审批上传；文件名使用安全附件响应，读取后 payload 仍可正常领取并批准。

### 2.4 文件快捷访问

1. 文件和目录 Pin 均通过 Host Filesystem 现存路径规范化。
2. JSON 返回 `path/label/href/kind`，文件链接包含父目录和 `focus_path`。
3. 自定义显示名校验非空与 128 字符上限。
4. 排序请求必须无重复且完整覆盖当前持久化列表。
5. 排序在刷新后保持，列表继续实例级全局共享。
6. 文件链接返回的文件页渲染唯一 `data-file-focus` 行。
7. schema 59 为旧固定项补 `target_kind=directory`，schema 20–58 均走显式兼容路径。
8. 文件菜单、编辑抽屉、拖动手柄与前端保存脚本契约均已覆盖。

### 2.5 全量回归

| 命令 | 结果 |
| --- | --- |
| `go test ./... -count=1` | 通过；全部 Go 包通过，`internal/web` 约 102 秒 |
| `npm test`（`integration/browser`） | 通过；全部契约测试与 Chromium desktop gate 通过 |
| `git diff --check` | 通过 |

## 3. 外部 Playwright/HTTP 黑盒条目

| # | 测试条目 | 结果 |
| --- | --- | --- |
| 1 | 登录页访问与管理员登录 | 通过；进入 `/monitor` |
| 2 | Monitor、Files、Quick Runs、External Interfaces 基础访问 | 通过；均为 200 |
| 3 | 排序按钮位于新建分组左侧 | 通过 |
| 4 | 页面不存在分组级排序按钮 | 通过 |
| 5 | 点击排序按钮不刷新页面 | 通过；页面内哨兵保持 |
| 6 | 快捷执行卡片进入 draggable 状态 | 通过；至少两个测试卡片 |
| 7 | 拖动快捷执行并完成排序 | 通过；刷新后顺序保持 |
| 8 | 文件“…”菜单 Pin 两个文件 | 通过；快捷访问出现两项 |
| 9 | 编辑图标打开侧边抽屉 | 通过；显示名改为 `Release notes` |
| 10 | 拖动文件快捷项 | 通过；刷新后顺序保持 |
| 11 | 点击文件快捷项 | 通过；进入父目录并出现唯一聚焦行 |
| 12 | 四类外部功能开启审批 | 通过；变量、日志、上传、快捷执行 |
| 13 | 新外部功能默认不审批 | 通过；日志调用立即返回 200 并落盘 |
| 14 | 需要审批的五次调用 | 通过；均返回 202 与 `pending_approval` |
| 15 | 审批前目标副作用 | 通过；变量未变、日志/上传未落盘、Quick Run 未启动 |
| 16 | 审批列表输入证据 | 通过；日志消息、上传名称/大小/SHA-256 和目标可见 |
| 17 | 拒绝上传 | 通过；目标文件不存在 |
| 18 | 批准变量 | 通过；`QA_APPROVAL=production` |
| 19 | 批准日志 | 通过；目标日志包含测试消息 |
| 20 | 批准上传 | 通过；目标内容与缓存内容一致 |
| 21 | 批准快捷执行 | 通过；Run 历史出现 `QA Quick One` |
| 22 | 重复批准 | 通过；第二次返回 409，未重复执行 |
| 23 | 旧上传收件箱 | 通过；路由 404，文件页无入口 |
| 24 | 浏览器运行时 | 通过；console error 与 page error 均为 0 |
| 25 | 每条审批的查看图标 | 通过；4/4 条均有 accessible icon link |
| 26 | 变量审批详情 | 通过；显示 `staging → production-candidate` |
| 27 | 日志审批详情 | 通过；完整显示待写日志消息与目标 |
| 28 | 上传审批详情 | 通过；显示缓存 JSON 内容、大小、SHA-256 和目标目录链接 |
| 29 | 上传同名判断 | 通过；既有文件保持不变，详情显示不覆盖并自动使用新文件名 |
| 30 | 快捷执行审批详情 | 通过；显示快捷执行名称、脚本路径、参数、超时和修订 |
| 31 | 审批详情响应式与运行时 | 通过；桌面无刷新抽屉、移动完整页面无横向溢出、console/page error 为 0 |
| 32 | 上传缓存直接下载 | 通过；附件内容、文件名与 `no-store` 正确，下载后仍保留 4 条待审批记录且目标文件未变化 |
| 33 | 批量覆盖冲突预览 | 通过；显示受影响的 `QA Batch Sync Retained` 及同步开关 |
| 34 | 勾选同步并覆盖两个文件 | 通过；结果显示同步 1 个快捷执行版本，配套文件一并保存 |
| 35 | 快捷执行版本与有效性 | 通过；版本由 v1 升至 v2，运行按钮保持可用 |
| 36 | 快捷执行运行详情标题与动作 | 通过；标题使用快捷执行名称，不显示“保存为快捷执行” |
| 37 | 合并详情与日志顺序 | 通过；技术字段位于合并详情内，详情在日志上方，旧折叠区不存在 |
| 38 | 重新执行 | 通过；按钮提交到原快捷执行并生成新的运行记录 |
| 39 | 新增流程浏览器运行时 | 通过；console error 与 page error 均为 0 |

## 4. 部署与证据

| 项目 | 结果 |
| --- | --- |
| 部署模式 | Windows 隔离便携部署 |
| 地址 | `http://127.0.0.1:5778` |
| 进程 | PID `25708`，最终构建复验时仍在监听 |
| Worktree | `D:\Github\worktrees\ScriptBoard\quick-run-inline-sort` |
| State Root | `.scratch/local-deploy-approval-quick-access-20260826/state` |
| 测试数据 | `D:\Github\ScriptBoard-QA\approval-quick-access-20260826` |
| 管理员 | `admin`；密码保留在 State Root 私有文件中 |
| 标准错误 | 0 字节 |
| 审计校验 | 85 条事件；哈希链和外部签名 checkpoint 有效 |

保留证据：

- `.scratch/local-deploy-approval-quick-access-20260826/blackbox-results.json`
- `.scratch/local-deploy-approval-quick-access-20260826/retained-smoke-results.json`
- `.scratch/local-deploy-approval-quick-access-20260826/qa.js`
- `.scratch/local-deploy-approval-quick-access-20260826/artifacts/quick-run-inline-reorder.png`
- `.scratch/local-deploy-approval-quick-access-20260826/artifacts/file-quick-access-focused.png`
- `.scratch/local-deploy-approval-quick-access-20260826/artifacts/external-approvals-pending.png`
- `.scratch/local-deploy-approval-quick-access-20260826/artifacts/external-activity-complete.png`
- `.scratch/local-deploy-approval-quick-access-20260826/approval-preview-blackbox-results.json`
- `.scratch/local-deploy-approval-quick-access-20260826/artifacts/external-approval-upload-preview.png`
- `.scratch/local-deploy-approval-quick-access-20260826/artifacts/external-approval-quick-run-preview.png`
- `.scratch/local-deploy-approval-quick-access-20260826/upload-sync-run-detail-results.json`
- `.scratch/local-deploy-approval-quick-access-20260826/artifacts/run-detail-merged-rerun.png`

## 5. 保留状态

- 最终构建重启后继续监听 `127.0.0.1:5778`（PID `25708`），原保留状态继续有效，新增批量上传同步与运行详情验证 7/7 通过；未停止或替换原有 `127.0.0.1:18788` 部署。
- 使用系统 Chrome 的外部 Playwright 驱动与 HTTP 请求上下文，没有使用应用内浏览器。
- State Root、审批/调用/审计记录、快捷执行、变量、快捷访问顺序、测试文件和截图全部保留。
- 初始管理员密码未写入报告或版本库，仅保留在部署 State Root 的秘密文件中。
