# Runtime resources are size- and digest-bound in capabilities.json and signed
# release manifests. Keep their bytes deterministic across host platforms.
/runtime/capabilities.json text eol=lf
/runtime/pi-runtime-lock.json text eol=lf
/runtime/scriptboard-extension.ts text eol=lf
/runtime/playbooks/*.md text eol=lf

# Keep the approved workflow preview byte-identical to its freeze manifest.
/docs/previews/workflow-platform.html -text
